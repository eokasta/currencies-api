# currencies-api

```java
final ServicesManager servicesManager = Bukkit.getServicesManager();
final PlayerWalletAPI playerWalletAPI = servicesManager.getRegistration(PlayerWalletAPI.class).getProvider();
final TopAPI topAPI = servicesManager.getRegistration(TopAPI.class).getProvider();
```
